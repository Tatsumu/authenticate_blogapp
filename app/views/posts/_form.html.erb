<%= form_for @posts do |f| %>

<p>
	<%= f.label :title %><br>
	<%= f.text_field :title %><br>
	<%= f.label :description %><br>
	<%= f.text_field :description %>
	<% if @posts.errors.any? %>
	<%= @posts.errors.messages[:title][0] %>
	<% end %>

</p>

<p>
	<%= f.submit %>
</p>

<% end %>
