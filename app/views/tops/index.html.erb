<h1>Users</h1>

<p><%= link_to "Singup", signup_path %>
<p><%= link_to "Login", signin_path %>
<p><%= link_to "Logout", signout_path %>
<% if current_user %>
  <p>メールアドレス<%= current_user.email %>でログインしています。</p>
<% end %>
<h1>ブログ一覧</h1>
	<ul>
		<li>
			<%= @post.title.each do |title| %><br>
			<%= post.title %><%= link_to "ブログへ", posts_path(post.id)%>
			<% end %>
		</li>
		<li></li>
		<li></li>
		<li></li>
		<li></li>
	</ul>